let fruits = ['banana', 'apple', 'watermelon'];

fruits.push('melon');

// fruits.pop()
// fruits.shift();


fruits.sort();

console.log(fruits.toString());

fruits.forEach(value => {
    console.log(value);
});